
export let headers = {
    'Content-Type': 'application/json;charset=utf-8'
};

export const enum CONST_TOKEN {
    HEADER = 'Authorization',
    PREFIX = 'Bearer',
    TOKEN = 'token',
    END_POINT = '/login'
}